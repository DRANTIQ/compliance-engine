policy_id: AWS_IAM_001
title: Root user must not have access keys
provider: aws
resource_type: identity.account
provider_type: aws_iam_account
severity: critical
description: Ensure the AWS root user does not have active access keys (CIS 2.3).
logic:
  type: fail_when
  condition:
    type: field_check
    path: properties.root_access_keys_present
    operator: gt
    expected: 0
evidence_fields:
- properties.account_id
- properties.root_access_keys_present
cis_control_id: '2.3'
remediation:
  headline: Root user must not have access keys
  risk_summary: Ensure the AWS root user does not have active access keys (CIS 2.3).
  business_impact: This misconfiguration may lead to unauthorized access or compliance
    failure.
  fix_summary: 'Remediate in AWS: Root user must not have access keys'
  estimated_fix_minutes: 5
  framework_mappings:
  - CIS 2.3
