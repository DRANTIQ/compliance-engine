policy_id: AWS_RDS_003
title: RDS instances must not be publicly accessible
provider: aws
resource_type: database.instance
provider_type: aws_rds_instance
severity: critical
description: RDS DB instances must not be publicly accessible (CIS 3.2.3).
logic:
  type: fail_when
  condition:
    type: field_check
    path: properties.publicly_accessible
    operator: eq
    expected: true
evidence_fields:
- properties.db_instance_identifier
- properties.publicly_accessible
remediation:
  headline: RDS instance publicly accessible
  risk_summary: RDS DB instances must not be publicly accessible (CIS 3.2.3).
  business_impact: This misconfiguration may lead to unauthorized access or compliance
    failure.
  fix_summary: 'Remediate in AWS: RDS instances must not be publicly accessible'
  estimated_fix_minutes: 5
  framework_mappings:
  - CIS 3.2.3
  - SOC2 CC6.4
display_title: RDS instance publicly accessible
pack_id: pack_aws_data
