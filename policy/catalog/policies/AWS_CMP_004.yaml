policy_id: AWS_CMP_004
version: 1.0.0
title: Lambda function must use a supported runtime
provider: aws
resource_type: compute.function
provider_type: aws_lambda_function
severity: high
description: Lambda function must use a supported runtime
logic:
  type: fail_when
  condition:
    type: field_check
    path: properties.runtime_deprecated
    operator: eq
    expected: true
evidence_fields:
- properties.function_name
- properties.runtime
remediation:
  headline: Lambda function uses deprecated runtime
  risk_summary: Lambda function must use a supported runtime
  business_impact: This misconfiguration may increase attack surface or reduce threat
    detection coverage.
  fix_summary: 'Remediate in AWS: Lambda function must use a supported runtime'
  estimated_fix_minutes: 5
  framework_mappings:
  - NIST SI-2
  - NIST SA-3
  - SOC2 CC6.8
display_title: Lambda function uses deprecated runtime
pack_id: pack_aws_compute_security
