policy_id: AWS_RDS_012
title: RDS snapshots must be encrypted
provider: aws
resource_type: database.snapshot
provider_type: aws_rds_snapshot
severity: high
description: RDS snapshots must be encrypted
logic:
  type: fail_when
  condition:
    type: field_check
    path: properties.encrypted
    operator: eq
    expected: false
evidence_fields:
- properties.db_snapshot_identifier
- properties.encrypted
remediation:
  headline: Unencrypted RDS snapshot
  risk_summary: RDS snapshots must be encrypted
  business_impact: This misconfiguration may increase attack surface or reduce threat
    detection coverage.
  fix_summary: 'Remediate in AWS: RDS snapshots must be encrypted'
  estimated_fix_minutes: 5
  framework_mappings:
  - NIST SC-28
  - SOC2 CC6.7
display_title: Unencrypted RDS snapshot
pack_id: pack_aws_data
