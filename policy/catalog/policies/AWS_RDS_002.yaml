policy_id: AWS_RDS_002
title: RDS auto minor version upgrade must be enabled
provider: aws
resource_type: database.instance
provider_type: aws_rds_instance
severity: medium
description: RDS DB instances must enable auto minor version upgrade (CIS 3.2.2).
logic:
  type: fail_when
  condition:
    type: field_check
    path: properties.auto_minor_version_upgrade
    operator: eq
    expected: false
evidence_fields:
- properties.db_instance_identifier
- properties.auto_minor_version_upgrade
remediation:
  headline: RDS auto minor version upgrade disabled
  risk_summary: RDS DB instances must enable auto minor version upgrade (CIS 3.2.2).
  business_impact: This misconfiguration may lead to unauthorized access or compliance
    failure.
  fix_summary: 'Remediate in AWS: RDS auto minor version upgrade must be enabled'
  estimated_fix_minutes: 3
  framework_mappings:
  - CIS 3.2.2
  - SOC2 CC8.1
display_title: RDS auto minor version upgrade disabled
pack_id: pack_aws_data
