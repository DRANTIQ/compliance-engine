policy_id: AWS_EFS_001
title: EFS file systems must be encrypted
provider: aws
resource_type: storage.filesystem
provider_type: aws_efs_file_system
severity: high
description: Amazon EFS file systems must be encrypted at rest (CIS 3.3.1).
logic:
  type: fail_when
  condition:
    type: field_check
    path: properties.encrypted
    operator: eq
    expected: false
evidence_fields:
- properties.file_system_id
- properties.encrypted
remediation:
  headline: EFS encryption disabled
  risk_summary: Amazon EFS file systems must be encrypted at rest (CIS 3.3.1).
  business_impact: This misconfiguration may lead to unauthorized access or compliance
    failure.
  fix_summary: 'Remediate in AWS: EFS file systems must be encrypted'
  estimated_fix_minutes: 5
  framework_mappings:
  - CIS 3.3.1
  - SOC2 CC6.7
display_title: EFS encryption disabled
pack_id: pack_aws_storage
