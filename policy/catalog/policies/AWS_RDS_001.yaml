policy_id: AWS_RDS_001
title: RDS instance encryption at rest must be enabled
provider: aws
resource_type: database.instance
provider_type: aws_rds_instance
severity: high
description: RDS DB instances must have storage encryption enabled (CIS 3.2.1).
logic:
  type: fail_when
  condition:
    type: field_check
    path: properties.storage_encrypted
    operator: eq
    expected: false
evidence_fields:
- properties.db_instance_identifier
- properties.storage_encrypted
remediation:
  headline: RDS storage encryption disabled
  risk_summary: RDS DB instances must have storage encryption enabled (CIS 3.2.1).
  business_impact: This misconfiguration may lead to unauthorized access or compliance
    failure.
  fix_summary: 'Remediate in AWS: RDS instance encryption at rest must be enabled'
  estimated_fix_minutes: 5
  framework_mappings:
  - CIS 3.2.1
  - SOC2 CC6.7
display_title: RDS storage encryption disabled
pack_id: pack_aws_data
version: 1.0.0
