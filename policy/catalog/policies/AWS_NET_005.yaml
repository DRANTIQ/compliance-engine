policy_id: AWS_NET_005
title: Security groups must not allow unrestricted admin ports (IPv6)
provider: aws
resource_type: network.security_group
provider_type: aws_ec2_security_group
severity: high
description: Security groups must not allow unrestricted SSH/RDP from ::/0 (CIS 6.4).
logic:
  type: fail_when
  condition:
    type: field_check
    path: properties.allows_unrestricted_admin_ports_ipv6
    operator: eq
    expected: true
evidence_fields:
- properties.group_id
- properties.group_name
- properties.allows_unrestricted_admin_ports_ipv6
remediation:
  headline: Security group allows admin ports (IPv6)
  risk_summary: Security groups must not allow unrestricted SSH/RDP from ::/0 (CIS
    6.4).
  business_impact: This misconfiguration may lead to unauthorized access or compliance
    failure.
  fix_summary: 'Remediate in AWS: Security groups must not allow unrestricted admin
    ports (IPv6)'
  estimated_fix_minutes: 5
  framework_mappings:
  - CIS 6.4
  - SOC2 CC6.4
display_title: Security group allows admin ports (IPv6)
pack_id: pack_aws_network
