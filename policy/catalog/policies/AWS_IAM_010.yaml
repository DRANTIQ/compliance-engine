policy_id: AWS_IAM_010
title: IAM users must not have full admin policies attached
provider: aws
resource_type: identity.user
provider_type: aws_iam_user
severity: high
description: IAM users must not have AdministratorAccess attached directly (CIS 2.15).
logic:
  type: fail_when
  condition:
    type: field_check
    path: properties.has_administrator_access
    operator: eq
    expected: true
evidence_fields:
- properties.user_name
- properties.has_administrator_access
remediation:
  headline: IAM user has administrator access
  risk_summary: IAM users must not have AdministratorAccess attached directly (CIS
    2.15).
  business_impact: This misconfiguration may lead to unauthorized access or compliance
    failure.
  fix_summary: 'Remediate in AWS: IAM users must not have full admin policies attached'
  estimated_fix_minutes: 5
  framework_mappings:
  - CIS 2.15
  - SOC2 CC6.3
display_title: IAM user has administrator access
pack_id: pack_aws_identity
